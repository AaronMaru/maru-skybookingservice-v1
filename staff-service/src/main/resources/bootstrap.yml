spring:
  profiles:
    active: ${PROFILE}
  application:
    name: staff-service
  cloud:
    config:
      uri: http://${CLOUD_SERVER}:8888
      
native-query:
  package-scan: com.skybooking.staffservice
