SELECT

    sui.id,
    sui.invite_to AS email

FROM stakeholder_user_invitations sui

WHERE stakeholder_company_id = :companyId
AND invite_stakeholder_user_id IS NULL

AND NOT EXISTS (SELECT * FROM users u where u.email = sui.invite_from)